/**
 * `_meta` for tool calls a widget makes on its own, so the server's per-call log
 * line can tell them from the model's calls (key read in src/mcp-core.ts).
 */
export const WIDGET_CALL_META = { 'transcriptor/source': 'widget' };
