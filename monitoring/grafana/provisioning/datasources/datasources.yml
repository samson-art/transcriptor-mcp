# Prometheus datasource for local / Docker Compose stacks.
# Override URL if Prometheus runs elsewhere.
apiVersion: 1
datasources:
  - name: Prometheus
    uid: prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
